<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="res_partner_inherit_form" model="ir.ui.view">
        <field name="name">res.partner.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <xpath expr="//group//group[1]" position="replace">
                <group invisible="not is_company">
                    <div colspan="2" class="d-flex gap-2"
                         invisible="not company_name or company_name == '' or is_company">
                        <label for="company_name" class="fw-bold text-nowrap"/>
                        <field name="company_name"/>
                        <button name="create_company" icon="fa-plus-square" string="Create" type="object"
                                class="oe_edit_only btn-link text-nowrap"/>
                    </div>
                    <span class="o_form_label o_td_label o_address_type" name="address_name">
                        <field name="type_address_label" invisible="is_company" readonly="1" class="fw-bold"/>
                        <span invisible="not is_company">Address</span>
                    </span>
                    <div class="o_address_format">
                        <field name="street" placeholder="Street..." class="o_address_street"/>
                        <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                        <field name="city" placeholder="City" class="o_address_city"/>
                        <field name="state_id" placeholder="State" class="o_address_state"
                               options="{'no_open': True, 'no_quick_create': True}"
                               context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                        <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                        <div name="partner_address_country" class="d-flex justify-content-between">
                            <field name="country_id" placeholder="Country" class="o_address_country"
                                   options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"/>
                        </div>
                    </div>
                </group>
                <group invisible="is_company">
                    <field name="parent_id" widget="res_partner_many2one" placeholder="Company Name..." string="Company"
                           domain="[('is_company', '=', True)]"
                           context="{'default_is_company': True, 'default_user_id': user_id, 'show_address': False}"
                           invisible="((is_company and not parent_id) or company_name) and company_name != ''"/>

                    <field name="customer_address"/>
                </group>
            </xpath>

        </field>
    </record>
</odoo>